# Phase 1 pipeline dependencies.
# Version floors, not exact pins — keeps things student-friendly while avoiding
# known-incompatible ancient releases.

pandas>=2.0
numpy>=1.24
yfinance>=0.2
statsmodels>=0.14
scikit-learn>=1.3
matplotlib>=3.7
pillow>=10.0
streamlit>=1.49
streamlit-echarts==0.4.0
requests>=2.31
