# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# Raw data (yfinance cache) IS committed so the panel is fully reproducible —
# yfinance adjusts historical prices over time (splits, dividends), which would
# otherwise cause the committed panel_phase1.csv to drift against a fresh run.
# To force a refresh, delete data/raw/*.csv and re-run the pipeline.

# Baseline artifacts from refactor verification
outputs/figures/_baseline/

# OS
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Local reference files (not tracked)
spec.md
proposal.md
DASHBOARD_README.md

# Generated outputs — committed so the Streamlit app can be hosted from GitHub
# (the data pipeline can't run on Streamlit Cloud). Regenerate locally with
# `MPLBACKEND=Agg python src/run.py`.
